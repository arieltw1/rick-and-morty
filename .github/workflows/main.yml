name: CI
on:
  push:
    branches:
      - main
jobs:
  job1:
    runs-on: ubuntu-latest
    name: build image and deploy to minikube
    steps:
    - uses: actions/checkout@v2
    - name: Start minikube
      uses: medyagh/setup-minikube@master
    - name: Try the cluster !
      run: kubectl get pods -A
    - name: Build image
      run: |
        export SHELL=/bin/bash
        eval $(minikube -p minikube docker-env)
        python3 code/gen_csv.py
        docker build . -t exercise:latest
    - name: Deploy to minikube
      run: |
        kubectl create namespace rick-and-morty
        kubectl apply -f yamls/
    - name: Test service URLs
      run: |
        minikube service list
        minikube service exercise-service -n rick-and-morty  --url
        echo "------------------opening the service------------------"
        curl $(minikube service exercise-service -n rick-and-morty --url)   
